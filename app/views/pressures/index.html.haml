%h3 
  Blood Presssures
  - if current_user
    = link_to 'New Pressure', new_pressure_path, class: 'btn btn-success float-right sized-button'

%table.table
  %thead
    %tr
      %th Sys
      %th Dias
      %th Pulse
      %th.time
      %th.edit


  %tbody
    - @pressures.each do |pressure|
      %tr
        %td= pressure.sys
        %td= pressure.dias
        %td= pressure.pulse
        %td= time_ago_title(pressure)
        %td.edit
          - if current_user == pressure.user
            = link_to 'Edit', edit_pressure_path(pressure)
          - else
            = link_to pressure.user.name, user_path(pressure.user)      
        

%br

